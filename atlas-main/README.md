# Atlas Wealth OS (app)

Next.js app for Atlas Wealth OS. **Setup, stack, and full project description** are in the [repository README](../README.md) at the repo root.

```bash
cd atlas-main
npm install
npm run dev
```

Dev server: [http://localhost:9002](http://localhost:9002).
